/* eslint-disable react/prop-types */

const Meal = ({ meal }) => {
  const {id, title} = meal
 
  return
  <div>
    <h1> id{id}</h1>
    <h1>name{title}</h1>
    
  </div>;
};

export default Meal;
